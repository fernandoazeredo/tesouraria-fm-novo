name: Aplicar regra global de campos obrigatorios

on:
  push:
    branches:
      - dev-fm-2026-09-08
    paths:
      - '.github/workflows/apply-global-required-validation.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar codigo
        uses: actions/checkout@v4
        with:
          ref: dev-fm-2026-09-08
          fetch-depth: 0

      - name: Aplicar regra global
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          def replace_once(path, old, new, label):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: esperado 1 trecho, encontrado {count}')
              p.write_text(text.replace(old, new, 1), encoding='utf-8')

          def regex_once(path, pattern, replacement, label, flags=0):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              new_text, count = re.subn(pattern, replacement, text, count=1, flags=flags)
              if count != 1:
                  raise SystemExit(f'{label}: esperado 1 trecho, encontrado {count}')
              p.write_text(new_text, encoding='utf-8')

          helper = '''type ValidationScope = string | HTMLElement

const SUMMARY_TEXT = 'Preencha os campos destacados para continuar.'
const FIELD_TEXT = 'Campo obrigatório'

function resolveScope(scope: ValidationScope) {
  return typeof scope === 'string' ? document.querySelector<HTMLElement>(scope) : scope
}

function wrapperOf(control: HTMLElement) {
  return control.closest<HTMLElement>('label') ?? control
}

function removeFieldState(control: HTMLElement) {
  const wrapper = wrapperOf(control)
  wrapper.classList.remove('field-validation-error')
  control.classList.remove('required-control-error')
  control.removeAttribute('aria-invalid')
  wrapper.querySelectorAll<HTMLElement>('[data-required-validation-message="true"]').forEach((item) => item.remove())
}

export function clearRequiredFieldErrors(scope: ValidationScope) {
  const host = resolveScope(scope)
  if (!host) return
  host.querySelectorAll<HTMLElement>('[data-required-key]').forEach(removeFieldState)
  host.querySelectorAll<HTMLElement>('[data-required-validation-summary="true"]').forEach((item) => item.remove())
}

export function showRequiredFieldErrors(scope: ValidationScope, keys: string[]) {
  const host = resolveScope(scope)
  if (!host) return false
  clearRequiredFieldErrors(host)
  const controls: HTMLElement[] = []
  for (const key of [...new Set(keys.filter(Boolean))]) {
    const control = host.querySelector<HTMLElement>(`[data-required-key="${key}"]`)
    if (!control) continue
    controls.push(control)
    const wrapper = wrapperOf(control)
    wrapper.classList.add('field-validation-error')
    control.classList.add('required-control-error')
    control.setAttribute('aria-invalid', 'true')
    if (wrapper.tagName === 'LABEL' && !wrapper.querySelector('[data-required-validation-message="true"]')) {
      const message = document.createElement('small')
      message.className = 'field-validation-message'
      message.dataset.requiredValidationMessage = 'true'
      message.textContent = FIELD_TEXT
      wrapper.appendChild(message)
    }
  }
  if (!controls.length) return false
  const first = controls[0]
  const wrapper = wrapperOf(first)
  const group = wrapper.closest<HTMLElement>('.form-grid, .obligation-form-grid, .settings-grid, .soc-settings-grid, .legacy-table, .labor-installments-table, .obligation-installments, .commission-deduction-table')
  const summary = document.createElement('div')
  summary.className = 'form-validation-summary'
  summary.dataset.requiredValidationSummary = 'true'
  summary.setAttribute('role', 'alert')
  summary.textContent = SUMMARY_TEXT
  if (group?.parentElement) group.parentElement.insertBefore(summary, group)
  else host.prepend(summary)
  window.requestAnimationFrame(() => {
    first.scrollIntoView({ behavior: 'smooth', block: 'center' })
    if (first instanceof HTMLInputElement || first instanceof HTMLSelectElement || first instanceof HTMLTextAreaElement) first.focus()
  })
  return true
}

if (typeof document !== 'undefined') {
  const clearEditedField = (event: Event) => {
    const target = event.target
    if (!(target instanceof HTMLElement)) return
    const control = target.closest<HTMLElement>('[data-required-key]')
    if (!control || control.getAttribute('aria-invalid') !== 'true') return
    const host = control.closest<HTMLElement>('.modal-sheet, .decision-modal, .page-card, .main-content, form')
    removeFieldState(control)
    if (host && !host.querySelector('[data-required-key][aria-invalid="true"]')) {
      host.querySelectorAll<HTMLElement>('[data-required-validation-summary="true"]').forEach((item) => item.remove())
    }
  }
  document.addEventListener('input', clearEditedField, true)
  document.addEventListener('change', clearEditedField, true)
}
'''
          Path('src/lib/requiredFieldValidation.ts').write_text(helper, encoding='utf-8')

          # CSS global único
          css_path = Path('src/styles.css')
          css = css_path.read_text(encoding='utf-8')
          css = css.replace('/* Validacao visual inline dos campos obrigatorios do cliente */', '/* Regra global de validacao visual dos campos obrigatorios */')
          marker = '/* Controles obrigatorios sem wrapper label */'
          if marker not in css:
              css += '''\n\n/* Controles obrigatorios sem wrapper label */\ninput.required-control-error, select.required-control-error, textarea.required-control-error { border-color: #dc2626 !important; background: rgba(220, 38, 38, .06) !important; box-shadow: 0 0 0 3px rgba(220, 38, 38, .12) !important; animation: required-field-pulse .45s ease-out; }\ninput.required-control-error:focus, select.required-control-error:focus, textarea.required-control-error:focus { border-color: #b91c1c !important; box-shadow: 0 0 0 4px rgba(220, 38, 38, .18) !important; }\n'''
          css_path.write_text(css, encoding='utf-8')

          # ALVARÁ
          p = 'src/pages/ReceivablesPageStorageV2.tsx'
          replace_once(p, "import { DEFAULT_BANK_ACCOUNT_ID, getBankAccount, type BankAccount } from '../data/bankAccounts'\n", "import { DEFAULT_BANK_ACCOUNT_ID, getBankAccount, type BankAccount } from '../data/bankAccounts'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'alvara-import')
          replace_once(p, "function BrazilianMoneyInput({ value, onChange, ariaLabel }: { value: number; onChange: (value: number) => void; ariaLabel?: string }) {", "function BrazilianMoneyInput({ value, onChange, ariaLabel, validationKey }: { value: number; onChange: (value: number) => void; ariaLabel?: string; validationKey?: string }) {", 'alvara-money-signature')
          replace_once(p, "    aria-label={ariaLabel}\n    placeholder=\"0,00\"", "    aria-label={ariaLabel}\n    data-required-key={validationKey}\n    placeholder=\"0,00\"", 'alvara-money-key')
          replace_once(p, "  const [showClientValidation, setShowClientValidation] = useState(false)\n  const [showProcessValidation, setShowProcessValidation] = useState(false)\n", '', 'alvara-remove-local-validation-state')
          regex_once(p, r"  async function save\(status: 'rascunho' \| 'enviado_tesouraria'\) \{.*?    const componentsToSave =", '''  async function save(status: 'rascunho' | 'enviado_tesouraria') {
    if (uploading) { window.alert('Aguarde o término do envio dos documentos.'); return }
    const missingKeys: string[] = []
    if (!processo.trim()) missingKeys.push('alvara-processo')
    if (!reclamante.trim()) missingKeys.push('alvara-reclamante')
    if (totalAlvara <= 0) missingKeys.push('alvara-total')
    if (status === 'enviado_tesouraria' && liquidoCliente > 0) {
      if (!banco.trim()) missingKeys.push('alvara-banco')
      if (!agencia.trim()) missingKeys.push('alvara-agencia')
      if (!conta.trim()) missingKeys.push('alvara-conta')
      if (!titular.trim()) missingKeys.push('alvara-titular')
      if (!cpf.trim()) missingKeys.push('alvara-cpf')
    }
    components.forEach((item, index) => {
      const requiresDetail = (item.nome.includes('Geral') || isDynamicComponent(item.nome)) && toNumber(item.valor) > 0 && !item.detalhe?.trim()
      if (requiresDetail) missingKeys.push(`alvara-detail-${index}`)
    })
    if (agentCommissionValue > 0 && !agentName.trim()) missingKeys.push('alvara-agent')
    if (missingKeys.length) { showRequiredFieldErrors('.revenue-sheet', missingKeys); return }
    clearRequiredFieldErrors('.revenue-sheet')
    const componentsToSave =''', 'alvara-save-validation', flags=re.S)
          regex_once(p, r"    <h3 className=\"form-section-title\">Dados do Processo</h3>\n    \{showProcessValidation.*?\n    <div className=\"form-grid compact-grid\">.*?</div>\n    <h3 className=\"form-section-title\">Composição do Valor</h3>", '''    <h3 className="form-section-title">Dados do Processo</h3>
    <div className="form-grid compact-grid"><label><span>Unidade</span><select value={unidade} onChange={(e) => setUnidade(e.target.value as 'RJ' | 'SP')}><option>RJ</option><option>SP</option></select></label><label><span>Data</span><input type="date" value={data} onChange={(e) => setData(e.target.value)} /></label><label><span>Natureza</span><select value={natureza} onChange={(e) => setNatureza(e.target.value)}><option>Trabalhista</option><option>Cível</option></select></label><label className="span-2"><span>Número do processo</span><input data-required-key="alvara-processo" value={processo} onChange={(e) => setProcesso(e.target.value)} /></label><label className="span-2"><span>Reclamada</span><input value={reclamada} onChange={(e) => setReclamada(e.target.value)} /></label><label className="span-2"><span>Reclamante</span><input data-required-key="alvara-reclamante" value={reclamante} onChange={(e) => setReclamante(e.target.value)} /></label><label><span>Origem</span><select value={origem} onChange={(e) => setOrigem(e.target.value)}><option>Alvará</option><option>Acordo</option></select></label><label><span>Forma de recebimento</span><input value={formaRecebimento} onChange={(e) => setFormaRecebimento(e.target.value)} /></label><label><span>Data prevista</span><input type="date" value={dataPrevista} onChange={(e) => setDataPrevista(e.target.value)} /></label></div>
    <h3 className="form-section-title">Composição do Valor</h3>''', 'alvara-process-ui', flags=re.S)
          replace_once(p, '<BrazilianMoneyInput value={totalAlvara} onChange={changeTotal} ariaLabel="Valor Líquido do Alvará" />', '<BrazilianMoneyInput value={totalAlvara} onChange={changeTotal} ariaLabel="Valor Líquido do Alvará" validationKey="alvara-total" />', 'alvara-total-key')
          replace_once(p, "<input aria-label={isAgentCommission ? 'Nome do agente ou beneficiário da comissão' : `Descrição ou beneficiário de ${componentLabel(component.nome)}`} placeholder={isAgentCommission ? 'Nome do agente / beneficiário da comissão' : 'Do que se trata / nome do beneficiário'} value={isAgentCommission ? agentName : component.detalhe ?? ''}", "<input data-required-key={isAgentCommission ? 'alvara-agent' : `alvara-detail-${index}`} aria-label={isAgentCommission ? 'Nome do agente ou beneficiário da comissão' : `Descrição ou beneficiário de ${componentLabel(component.nome)}`} placeholder={isAgentCommission ? 'Nome do agente / beneficiário da comissão' : 'Do que se trata / nome do beneficiário'} value={isAgentCommission ? agentName : component.detalhe ?? ''}", 'alvara-detail-keys')
          regex_once(p, r"    <h3 className=\"form-section-title\">Dados bancários para crédito do cliente</h3>\n    \{showClientValidation.*?\n    <div className=\"form-grid compact-grid\">.*?</div>\n    <h3 className=\"form-section-title\">Dados para emissão de Nota Fiscal</h3>", '''    <h3 className="form-section-title">Dados bancários para crédito do cliente</h3>
    <div className="form-grid compact-grid">
      <label><span>Banco *</span><input data-required-key="alvara-banco" value={banco} required={liquidoCliente > 0} onChange={(e) => setBanco(e.target.value)} /></label>
      <label><span>Agência *</span><input data-required-key="alvara-agencia" value={agencia} required={liquidoCliente > 0} onChange={(e) => setAgencia(e.target.value)} /></label>
      <label><span>Conta *</span><input data-required-key="alvara-conta" value={conta} required={liquidoCliente > 0} onChange={(e) => setConta(e.target.value)} /></label>
      <label className="span-2"><span>Nome / Titular *</span><input data-required-key="alvara-titular" value={titular} required={liquidoCliente > 0} onChange={(e) => setTitular(e.target.value)} /></label>
      <label><span>CPF *</span><input data-required-key="alvara-cpf" value={cpf} required={liquidoCliente > 0} onChange={(e) => setCpf(e.target.value)} /></label>
      <label className="span-2"><span>PIX (opcional)</span><input value={pix} onChange={(e) => setPix(e.target.value)} /></label>
    </div>
    <h3 className="form-section-title">Dados para emissão de Nota Fiscal</h3>''', 'alvara-client-ui', flags=re.S)

          # ACORDO TRABALHISTA
          p = 'src/components/LaborAgreementReceivable.tsx'
          replace_once(p, "import { DEFAULT_BANK_ACCOUNT_ID, getBankAccount, type BankAccount } from '../data/bankAccounts'\n", "import { DEFAULT_BANK_ACCOUNT_ID, getBankAccount, type BankAccount } from '../data/bankAccounts'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'acordo-import')
          replace_once(p, "function MoneyInput({ value, onChange, ariaLabel }: { value: number; onChange: (value: number) => void; ariaLabel?: string }) {", "function MoneyInput({ value, onChange, ariaLabel, validationKey }: { value: number; onChange: (value: number) => void; ariaLabel?: string; validationKey?: string }) {", 'acordo-money-signature')
          replace_once(p, "    aria-label={ariaLabel}\n    placeholder=\"0,00\"", "    aria-label={ariaLabel}\n    data-required-key={validationKey}\n    placeholder=\"0,00\"", 'acordo-money-key')
          replace_once(p, "  const [showClientValidation, setShowClientValidation] = useState(false)\n  const [showProcessValidation, setShowProcessValidation] = useState(false)\n", '', 'acordo-remove-local-validation-state')
          regex_once(p, r"  async function save\(status: 'rascunho' \| 'enviado_tesouraria'\) \{.*?    setBusy\(true\)", '''  async function save(status: 'rascunho' | 'enviado_tesouraria') {
    const missingKeys: string[] = []
    if (!processo.trim()) missingKeys.push('acordo-processo')
    if (!reclamante.trim()) missingKeys.push('acordo-reclamante')
    if (valorAcordoBruto <= 0) missingKeys.push('acordo-valor-bruto')
    if (status === 'enviado_tesouraria' && totalRecebido <= 0) {
      const firstIncomplete = parcelas.findIndex((row) => !row.dataRealizada || row.valorParcela <= 0)
      const index = firstIncomplete >= 0 ? firstIncomplete : 0
      if (!parcelas[index]?.dataRealizada) missingKeys.push(`acordo-realizada-${index}`)
      if (toNumber(parcelas[index]?.valorParcela) <= 0) missingKeys.push(`acordo-parcela-valor-${index}`)
    }
    if (status === 'enviado_tesouraria' && liquidoClienteRecebido > 0) {
      if (!bancoCliente.trim()) missingKeys.push('acordo-banco')
      if (!agenciaCliente.trim()) missingKeys.push('acordo-agencia')
      if (!contaCliente.trim()) missingKeys.push('acordo-conta')
      if (!titularCliente.trim()) missingKeys.push('acordo-titular')
      if (!cpfCliente.trim()) missingKeys.push('acordo-cpf')
      if (!emailCliente.trim()) missingKeys.push('acordo-email')
      if (!telefoneCliente.trim()) missingKeys.push('acordo-telefone')
      if (!enderecoCliente.trim()) missingKeys.push('acordo-endereco')
    }
    if (outrasDeducoes > 0 && !outrasDeducoesDescricao.trim()) missingKeys.push('acordo-outras-deducoes')
    if (missingKeys.length) { showRequiredFieldErrors('.labor-agreement-sheet', missingKeys); return }
    clearRequiredFieldErrors('.labor-agreement-sheet')
    setBusy(true)''', 'acordo-save-validation', flags=re.S)
          regex_once(p, r"    <h3 className=\"form-section-title\">Dados do Processo</h3>\n    \{showProcessValidation.*?\n    <div className=\"form-grid compact-grid labor-agreement-grid\">.*?</div>\n\n    <h3 className=\"form-section-title\">Dados do Acordo</h3>", '''    <h3 className="form-section-title">Dados do Processo</h3>
    <div className="form-grid compact-grid labor-agreement-grid">
      <label><span>Unidade</span><select value={unidade} onChange={(e) => setUnidade(e.target.value as 'RJ' | 'SP')}><option>RJ</option><option>SP</option></select></label>
      <label><span>Data</span><input type="date" value={data} onChange={(e) => setData(e.target.value)} /></label>
      <label><span>Natureza</span><input value="Trabalhista" readOnly /></label>
      <label className="span-2"><span>Número do processo</span><input data-required-key="acordo-processo" value={processo} onChange={(e) => setProcesso(e.target.value)} /></label>
      <label className="span-2"><span>Reclamada</span><input value={reclamada} onChange={(e) => setReclamada(e.target.value)} /></label>
      <label className="span-2"><span>Reclamante</span><input data-required-key="acordo-reclamante" value={reclamante} onChange={(e) => setReclamante(e.target.value)} /></label>
      <label><span>Percentual Honorários (%)</span><input type="number" min="0" max="100" step="0.01" value={percentualHonorarios} onChange={(e) => setPercentualHonorarios(Math.max(0, Number(e.target.value) || 0))} /></label>
    </div>

    <h3 className="form-section-title">Dados do Acordo</h3>''', 'acordo-process-ui', flags=re.S)
          replace_once(p, '<MoneyInput value={valorAcordoBruto} onChange={setValorAcordoBruto} ariaLabel="Valor bruto do acordo" />', '<MoneyInput value={valorAcordoBruto} onChange={setValorAcordoBruto} ariaLabel="Valor bruto do acordo" validationKey="acordo-valor-bruto" />', 'acordo-gross-key')
          replace_once(p, '<input type="date" value={row.dataRealizada} onChange={(e) => updateParcela(index, { dataRealizada: e.target.value })} />', '<input data-required-key={`acordo-realizada-${index}`} type="date" value={row.dataRealizada} onChange={(e) => updateParcela(index, { dataRealizada: e.target.value })} />', 'acordo-realized-key')
          replace_once(p, '<MoneyInput value={row.valorParcela} onChange={(value) => updateParcela(index, { valorParcela: value })} ariaLabel={`Valor parcela ${row.numero}`} />', '<MoneyInput value={row.valorParcela} onChange={(value) => updateParcela(index, { valorParcela: value })} ariaLabel={`Valor parcela ${row.numero}`} validationKey={`acordo-parcela-valor-${index}`} />', 'acordo-installment-value-key')
          regex_once(p, r"    <h3 className=\"form-section-title\">Dados bancários e contato do cliente</h3>\n    \{showClientValidation.*?\n    <div className=\"form-grid compact-grid labor-agreement-grid\">.*?</div>\n\n    <h3 className=\"form-section-title\">Deduções</h3>", '''    <h3 className="form-section-title">Dados bancários e contato do cliente</h3>
    <div className="form-grid compact-grid labor-agreement-grid">
      <label><span>Banco *</span><input data-required-key="acordo-banco" value={bancoCliente} required={liquidoClienteRecebido > 0} onChange={(e) => setBancoCliente(e.target.value)} /></label>
      <label><span>Agência *</span><input data-required-key="acordo-agencia" value={agenciaCliente} required={liquidoClienteRecebido > 0} onChange={(e) => setAgenciaCliente(e.target.value)} /></label>
      <label><span>Conta Corrente *</span><input data-required-key="acordo-conta" value={contaCliente} required={liquidoClienteRecebido > 0} onChange={(e) => setContaCliente(e.target.value)} /></label>
      <label><span>Nome / Titular *</span><input data-required-key="acordo-titular" value={titularCliente} required={liquidoClienteRecebido > 0} onChange={(e) => setTitularCliente(e.target.value)} /></label>
      <label><span>CPF *</span><input data-required-key="acordo-cpf" value={cpfCliente} required={liquidoClienteRecebido > 0} onChange={(e) => setCpfCliente(e.target.value)} /></label>
      <label><span>PIX (opcional)</span><input value={pixCliente} onChange={(e) => setPixCliente(e.target.value)} /></label>
      <label><span>E-mail *</span><input data-required-key="acordo-email" type="email" value={emailCliente} required={liquidoClienteRecebido > 0} onChange={(e) => setEmailCliente(e.target.value)} /></label>
      <label><span>Telefone *</span><input data-required-key="acordo-telefone" value={telefoneCliente} required={liquidoClienteRecebido > 0} onChange={(e) => setTelefoneCliente(e.target.value)} /></label>
      <label className="span-2"><span>Endereço *</span><input data-required-key="acordo-endereco" value={enderecoCliente} required={liquidoClienteRecebido > 0} onChange={(e) => setEnderecoCliente(e.target.value)} /></label>
    </div>

    <h3 className="form-section-title">Deduções</h3>''', 'acordo-client-ui', flags=re.S)
          replace_once(p, '<label className="span-2"><span>Especificar outras deduções</span><input value={outrasDeducoesDescricao}', '<label className="span-2"><span>Especificar outras deduções</span><input data-required-key="acordo-outras-deducoes" value={outrasDeducoesDescricao}', 'acordo-deduction-key')

          # OUTRAS RECEITAS
          p = 'src/components/OtherRevenueReceivable.tsx'
          replace_once(p, "import { officialChartOfAccounts } from '../data/chartOfAccounts'\n", "import { officialChartOfAccounts } from '../data/chartOfAccounts'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'other-import')
          replace_once(p, "function MoneyInput({ value, onChange }: { value: number; onChange: (value: number) => void }) {", "function MoneyInput({ value, onChange, validationKey }: { value: number; onChange: (value: number) => void; validationKey?: string }) {", 'other-money-signature')
          replace_once(p, '    aria-label="Valor da outra receita"\n    placeholder="0,00"', '    aria-label="Valor da outra receita"\n    data-required-key={validationKey}\n    placeholder="0,00"', 'other-money-key')
          replace_once(p, "    if (!descricao.trim() || valor <= 0 || !selectedAccount) {\n      window.alert('Preencha descrição/origem, valor e conta gerencial.')\n      return\n    }", "    const missingKeys: string[] = []\n    if (!descricao.trim()) missingKeys.push('other-description')\n    if (valor <= 0) missingKeys.push('other-value')\n    if (!selectedAccount) missingKeys.push('other-account')\n    if (missingKeys.length) { showRequiredFieldErrors('.other-revenue-sheet', missingKeys); return }\n    clearRequiredFieldErrors('.other-revenue-sheet')", 'other-save-validation')
          replace_once(p, '<label><span>Valor</span><MoneyInput value={valor} onChange={setValor} /></label>', '<label><span>Valor</span><MoneyInput value={valor} onChange={setValor} validationKey="other-value" /></label>', 'other-value-key')
          replace_once(p, '<label className="span-3"><span>Descrição / Origem</span><input value={descricao}', '<label className="span-3"><span>Descrição / Origem</span><input data-required-key="other-description" value={descricao}', 'other-description-key')
          replace_once(p, '<label className="span-3"><span>Conta Gerencial</span><select value={accountCode}', '<label className="span-3"><span>Conta Gerencial</span><select data-required-key="other-account" value={accountCode}', 'other-account-key')

          # DESPESAS
          p = 'src/pages/ExpensesPageStorage.tsx'
          replace_once(p, "import type { ChartOfAccount } from '../data/chartOfAccounts'\n", "import type { ChartOfAccount } from '../data/chartOfAccounts'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'expenses-import')
          replace_once(p, "    if (!nome.trim() || !validItems.length || total <= 0) {\n      window.alert('Preencha o nome/responsável e pelo menos uma linha de despesa com histórico e valor.')\n      return\n    }", "    const missingKeys: string[] = []\n    if (!nome.trim()) missingKeys.push('expense-name')\n    const firstValidIndex = items.findIndex((item) => item.historico.trim() || parseBRL(item.valor) > 0)\n    const requiredIndex = firstValidIndex >= 0 ? firstValidIndex : 0\n    if (!items[requiredIndex]?.historico.trim()) missingKeys.push(`expense-history-${requiredIndex}`)\n    if (parseBRL(items[requiredIndex]?.valor || '') <= 0 || total <= 0) missingKeys.push(`expense-value-${requiredIndex}`)\n    if (missingKeys.length) { showRequiredFieldErrors('.expense-sheet', missingKeys); return }\n    clearRequiredFieldErrors('.expense-sheet')", 'expenses-save-validation')
          replace_once(p, '<label className="span-2"><span>Nome / Responsável</span><input value={nome}', '<label className="span-2"><span>Nome / Responsável</span><input data-required-key="expense-name" value={nome}', 'expenses-name-key')
          replace_once(p, '<input value={item.historico} onChange={(e) => updateItem(index, \'historico\', e.target.value)} />', '<input data-required-key={`expense-history-${index}`} value={item.historico} onChange={(e) => updateItem(index, \'historico\', e.target.value)} />', 'expenses-history-key')
          replace_once(p, '<input inputMode="numeric" value={item.valor} onChange={(e) => updateItem(index, \'valor\', formatCurrencyFromDigits(e.target.value))} placeholder="R$ 0,00" />', '<input data-required-key={`expense-value-${index}`} inputMode="numeric" value={item.valor} onChange={(e) => updateItem(index, \'valor\', formatCurrencyFromDigits(e.target.value))} placeholder="R$ 0,00" />', 'expenses-value-key')

          # NFS-e
          p = 'src/pages/FiscalNotesPageV3.tsx'
          replace_once(p, "import { useAuth } from '../auth/AuthContext'\n", "import { useAuth } from '../auth/AuthContext'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'fiscal-import')
          replace_once(p, "    if (status === 'emitida' && (!number.trim() || !issueDate)) {\n      window.alert('Para marcar como Emitida, informe o número da NFS-e e a data de emissão.')\n      return\n    }", "    if (status === 'emitida') {\n      const missingKeys: string[] = []\n      if (!number.trim()) missingKeys.push('fiscal-number')\n      if (!issueDate) missingKeys.push('fiscal-date')\n      if (missingKeys.length) { showRequiredFieldErrors('.obligation-modal', missingKeys); return }\n    }\n    clearRequiredFieldErrors('.obligation-modal')", 'fiscal-save-validation')
          replace_once(p, '<label><span>Número da NFS-e</span><input value={number}', '<label><span>Número da NFS-e</span><input data-required-key="fiscal-number" value={number}', 'fiscal-number-key')
          replace_once(p, '<label><span>Data de emissão</span><input type="date" value={issueDate}', '<label><span>Data de emissão</span><input data-required-key="fiscal-date" type="date" value={issueDate}', 'fiscal-date-key')

          # APROVAÇÕES
          p = 'src/pages/ApprovalsPageEnhanced.tsx'
          replace_once(p, "import { WorkflowStatusBadge } from '../components/WorkflowStatusBadge'\n", "import { WorkflowStatusBadge } from '../components/WorkflowStatusBadge'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'approval-import')
          replace_once(p, "  async function confirmDecision() {\n    if (!canDecide || !decision || !reason.trim()) return\n", "  async function confirmDecision() {\n    if (!canDecide || !decision) return\n    if (!reason.trim()) { showRequiredFieldErrors('.decision-modal', ['approval-reason']); return }\n    clearRequiredFieldErrors('.decision-modal')\n", 'approval-validation')
          replace_once(p, '<textarea rows={5} autoFocus value={reason}', '<textarea data-required-key="approval-reason" rows={5} autoFocus value={reason}', 'approval-reason-key')
          replace_once(p, 'disabled={busy || !reason.trim()} onClick={confirmDecision}', 'disabled={busy} onClick={confirmDecision}', 'approval-button')

          # CONFIGURAÇÕES INSTITUCIONAIS
          p = 'src/components/InstitutionalSettingsEditor.tsx'
          replace_once(p, "import { db } from '../lib/firebase'\n", "import { db } from '../lib/firebase'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'settings-import')
          replace_once(p, "    if (!form.razaoSocial.trim() || !form.cnpj.trim() || !form.endereco.trim()) {\n      setMessage('Preencha todos os campos.')\n      return\n    }", "    const missingKeys: string[] = []\n    if (!form.razaoSocial.trim()) missingKeys.push('institution-name')\n    if (!form.cnpj.trim()) missingKeys.push('institution-cnpj')\n    if (!form.endereco.trim()) missingKeys.push('institution-address')\n    if (missingKeys.length) { showRequiredFieldErrors('.institutional-settings-editor', missingKeys); return }\n    clearRequiredFieldErrors('.institutional-settings-editor')", 'settings-validation')
          replace_once(p, '<label><span>Razão Social</span><input value={form.razaoSocial}', '<label><span>Razão Social</span><input data-required-key="institution-name" value={form.razaoSocial}', 'settings-name-key')
          replace_once(p, '<label><span>CNPJ</span><input value={form.cnpj}', '<label><span>CNPJ</span><input data-required-key="institution-cnpj" value={form.cnpj}', 'settings-cnpj-key')
          replace_once(p, '<label className="settings-full-width"><span>Endereço</span><input value={form.endereco}', '<label className="settings-full-width"><span>Endereço</span><input data-required-key="institution-address" value={form.endereco}', 'settings-address-key')

          # REPASSE SOCIETÁRIO - campo obrigatório de beneficiário
          p = 'src/pages/SocietaryTransfersPage.tsx'
          replace_once(p, "import { isOfficialDirectorEmail, useAuth } from '../auth/AuthContext'\n", "import { isOfficialDirectorEmail, useAuth } from '../auth/AuthContext'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'soc-import')
          replace_once(p, "    if (!form.beneficiary.trim() || form.defaultPercent < 0 || form.defaultPercent > 100 || form.dueDay < 0 || form.dueDay > 31) {\n      window.alert('Revise beneficiário, percentual padrão e dia de vencimento.')\n      return\n    }", "    if (!form.beneficiary.trim()) { showRequiredFieldErrors('.soc-settings', ['soc-beneficiary']); return }\n    clearRequiredFieldErrors('.soc-settings')\n    if (form.defaultPercent < 0 || form.defaultPercent > 100 || form.dueDay < 0 || form.dueDay > 31) {\n      window.alert('Revise beneficiário, percentual padrão e dia de vencimento.')\n      return\n    }", 'soc-validation')
          replace_once(p, '<label><span>Beneficiário</span><input value={form.beneficiary}', '<label><span>Beneficiário</span><input data-required-key="soc-beneficiary" value={form.beneficiary}', 'soc-beneficiary-key')

          # REPASSE DE ALVARÁS - parcelas obrigatórias
          p = 'src/pages/AlvaraControlPagesV3.tsx'
          replace_once(p, "import { isOfficialDirectorEmail, useAuth } from '../auth/AuthContext'\n", "import { isOfficialDirectorEmail, useAuth } from '../auth/AuthContext'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'transfer-import')
          replace_once(p, "    if (!installments.length || installments.some((item) => !item.dueDate || toNumber(item.value) <= 0)) { window.alert('Informe valor e data de todas as parcelas.'); return }", "    if (!installments.length || installments.some((item) => !item.dueDate || toNumber(item.value) <= 0)) {\n      const missingKeys: string[] = []\n      installments.forEach((item, index) => { if (toNumber(item.value) <= 0) missingKeys.push(`transfer-value-${index}`); if (!item.dueDate) missingKeys.push(`transfer-date-${index}`) })\n      if (missingKeys.length) { showRequiredFieldErrors('.obligation-modal', missingKeys); return }\n    }\n    clearRequiredFieldErrors('.obligation-modal')", 'transfer-installment-validation')
          replace_once(p, '<input type="number" min="0" step="0.01" value={item.value}', '<input data-required-key={`transfer-value-${index}`} type="number" min="0" step="0.01" value={item.value}', 'transfer-value-key')
          replace_once(p, '<input type="date" value={item.dueDate}', '<input data-required-key={`transfer-date-${index}`} type="date" value={item.dueDate}', 'transfer-date-key')

          # COMISSÕES DE AGENTES - deduções e parcelas obrigatórias
          p = 'src/pages/AgentCommissionsPageV4.tsx'
          replace_once(p, "import { isOfficialDirectorEmail, useAuth } from '../auth/AuthContext'\n", "import { isOfficialDirectorEmail, useAuth } from '../auth/AuthContext'\nimport { clearRequiredFieldErrors, showRequiredFieldErrors } from '../lib/requiredFieldValidation'\n", 'commission-import')
          replace_once(p, "function MoneyInputBR({ value, onChange, disabled = false, ariaLabel }: { value: number; onChange: (value: number) => void; disabled?: boolean; ariaLabel?: string }) {", "function MoneyInputBR({ value, onChange, disabled = false, ariaLabel, validationKey }: { value: number; onChange: (value: number) => void; disabled?: boolean; ariaLabel?: string; validationKey?: string }) {", 'commission-money-signature')
          replace_once(p, '  return <input type="text" inputMode="decimal" aria-label={ariaLabel} disabled={disabled}', '  return <input type="text" inputMode="decimal" aria-label={ariaLabel} data-required-key={validationKey} disabled={disabled}', 'commission-money-key')
          replace_once(p, "    if (deductions.some((item) => item.value > 0 && (!item.description.trim() || !item.date))) { window.alert('Informe data e descrição para todas as deduções com valor.'); return }", "    const missingKeys: string[] = []\n    deductions.forEach((item, index) => { if (item.value > 0 && !item.date) missingKeys.push(`commission-deduction-date-${index}`); if (item.value > 0 && !item.description.trim()) missingKeys.push(`commission-deduction-description-${index}`) })\n    installments.forEach((item, index) => { if (item.value <= 0) missingKeys.push(`commission-installment-value-${index}`); if (!item.dueDate) missingKeys.push(`commission-installment-date-${index}`) })\n    if (missingKeys.length) { showRequiredFieldErrors('.commission-v4-modal', missingKeys); return }\n    clearRequiredFieldErrors('.commission-v4-modal')", 'commission-required-validation')
          replace_once(p, "    if (installments.some((item) => !item.dueDate || item.value <= 0)) { window.alert('Informe valor e data de todas as parcelas.'); return }\n", '', 'commission-remove-installment-alert')
          replace_once(p, '<input type="date" value={item.date} disabled={!canOperate || !editable}', '<input data-required-key={`commission-deduction-date-${index}`} type="date" value={item.date} disabled={!canOperate || !editable}', 'commission-deduction-date-key')
          replace_once(p, '<input value={item.description} disabled={!canOperate || !editable}', '<input data-required-key={`commission-deduction-description-${index}`} value={item.description} disabled={!canOperate || !editable}', 'commission-deduction-description-key')
          replace_once(p, '<MoneyInputBR value={item.value} disabled={execution || !canOperate || !editable} onChange={(value) => patchInstallment(index, { value })} />', '<MoneyInputBR value={item.value} disabled={execution || !canOperate || !editable} validationKey={`commission-installment-value-${index}`} onChange={(value) => patchInstallment(index, { value })} />', 'commission-installment-value-key')
          replace_once(p, '<input type="date" value={item.dueDate} disabled={execution || !canOperate || !editable}', '<input data-required-key={`commission-installment-date-${index}`} type="date" value={item.dueDate} disabled={execution || !canOperate || !editable}', 'commission-installment-date-key')
          PY

      - name: Remover workflow temporario
        run: rm .github/workflows/apply-global-required-validation.yml

      - name: Instalar dependencias
        run: npm install --no-package-lock

      - name: Validar build
        run: npm run build

      - name: Commitar e publicar
        shell: bash
        run: |
          git config user.name "Fernando Azeredo - automacao"
          git config user.email "132702186+fernandoazeredo@users.noreply.github.com"
          git add src .github/workflows/apply-global-required-validation.yml
          git commit -m "Implementa regra global de campos obrigatorios"
          git push origin HEAD:dev-fm-2026-09-08
